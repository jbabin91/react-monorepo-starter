import { createStyle } from '@gluestack-style/react';

export const AvatarBadge = createStyle({
  bg: '$success500',
  borderColor: 'white',
  borderRadius: '$full',
  borderWidth: 2,
  bottom: 0,
  h: '$5',
  position: 'absolute',
  right: 0,
  w: '$5',
});
